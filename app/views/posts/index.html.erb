<ul>
  <% @posts.each do |post| %>
  <li><%= link_to post.name, post_path(post) %></li>
  <p>
    <%= post.body %>
    <%= link_to "Delete", post_path(post), method: :delete %>
  </p>
  <% end %>
</ul>
